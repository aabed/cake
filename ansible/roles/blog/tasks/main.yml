- name: Container is running
  docker:
    state: reloaded
    restart_policy: always
    image: wordpress
    name: blog
    ports:
      - "{{ wp_port }}:80"
    links:
      - db:mysql
    log_driver: syslog
    log_opt:
      syslog-tag: blog
  tags: [blog]
